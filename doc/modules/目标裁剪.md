# 目标裁剪 TargetCrop

> 模块组：管道模块 | 代码：`umvp/pipe/target_crop.py`（复用 `umvp/pipe/cropper.py` 的 `Cropper`）
> 数据库参数：`CROP0001`「目标裁剪·默认」（presets 表 + preset_params_target_crop 表）

## 1. 模块定位

按坐标裁图的**通用模块**：接收「原帧 + 坐标」（通常来自上游 YOLO 检测框），按框裁出子图；
输出分辨率可配（`out_size`，默认原图）。**不含任何检测器**，不做目标有效性过滤
（置信度/尺寸过滤由上游识别模块在其输出处负责）——`classes`/`filter` 只决定"裁哪些框"。

原「原分辨率提取 extract_faces」已拆分：裁 YOLO 框的机械裁剪归本模块；检脸 + 原图出脸
归**人脸检测**模块（`face_detect.face_pipeline.FaceStage`）。车牌链的"裁车"同样归本模块。

## 2. 参数

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| out_size | str | "" | 输出分辨率：空=原图；`640`=长边像素等比；`640,480`=严格尺寸；`×2`/`2x`=缩放倍数 |
| margin | float | 0.0 | 裁剪外扩比例（按框宽高外扩，clamp 到图边界） |
| classes | str | "" | 只裁这些类别（逗号分隔，空=全部；如 2=车，0=人） |
| filter | str | "" | 框筛选条件 DSL（显式字段名；空=全裁，即保持原简单过滤）：组内逗号 AND、组间分号 OR；操作符 `== != > >= < <= in`（`=`≡`==`；`in` 值用 `|`）。字段 `cls_id`/`track_id`/`score`（别名 `class`/`类别`、`track`、`conf`）。例：`track_id == 1`、`cls_id in 0|2, score >= 0.5` |

`classes` 与 `filter` 同时生效（AND）；条件 DSL 与「标注」模块共用 `pipe.conditions`。

`image`、`bbox` 为模块测试页参数（test_only，不落库、不进链路）。

## 3. 输入 / 输出

`crop_targets(frame, dets=None, boxes=None, *, margin=0.0, classes=None, filter="", out_size=None)`

| 输入 | 说明 |
|---|---|
| frame | 原帧（一切坐标的基准） |
| dets | 上游检测结果（取 bbox/cls_id/score/track_id） |
| boxes | 裸框列表（无检测元数据）；与 dets 二选一 |

输出 `list[CropResult]`：

| 字段 | 类型 | 内容 |
|---|---|---|
| img | ndarray | 裁剪子图（按 out_size 缩放后） |
| bbox | (x1,y1,x2,y2) int | 实际裁剪框（原图坐标，含 margin，clamp 后） |
| offset | (ox,oy) int | 子图左上角在原图坐标 |
| scale | float | 输出缩放倍数（1.0=原分辨率） |
| cls_id / score / track_id | int/float/int | 来源检测元数据（无检测时 cls=-1） |
| index | int | 序号 |

方法：`to_original(bbox)` / `kps_to_original(kps)` —— 子图坐标 → 原图坐标
（除以 scale 加 offset）；下游识别模块据此还原车牌/人脸框并出原分辨率小图。

## 4. 上下游衔接

- 上游：YOLO 检测框（人/车）或任意坐标。
- 下游：
  - **车牌识别**（`plate_recog.extract_plates`）：在车图里裁车牌 → 还原坐标 → 原图出牌。
  - **人脸检测**（`face_detect.face_pipeline.FaceStage`）：在人图里检脸 → 还原坐标 → 原图出脸。
  - **VLM**：需要送该类 ROI 裁剪时，YOLO 之后插本模块。
- 装配：`web_lab/server.py` 的 `_chain_from_spec` 构造 `TargetCrop` 注入 `Pipeline.crop`；
  `Pipeline.step` 在 YOLO 之后按 `check_interval` 门控产出 `last_crops`。

## 5. 保留链路

- **PIPE0002 车牌识别链路**：帧管理 → YOLO识车 → **目标裁剪(裁车)** → 车牌识别 → 告警。
- **PIPE0003 人脸截取链路**：帧管理 → YOLO识人 → **目标裁剪(裁人)** → 人脸检测（检脸+原图出脸）。

## 6. 验证

```bash
conda run -n ai python tests/test_target_crop.py   # 35 断言（按坐标裁图/分辨率(长边/严格/倍数)/外扩/过滤(含条件 DSL)/坐标还原）
conda run -n ai python tests/test_cropper.py       # Cropper 纯逻辑 26 断言
```
